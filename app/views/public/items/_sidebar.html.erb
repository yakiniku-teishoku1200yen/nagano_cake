<div>
  ジャンル検索
</div>
  <table>
    <tr>
      <td>
        <%= link_to "全商品を表示", items_path %>
      </td>
    </tr>

    <% genres.each do |genre| %>
      <tr>
        <td>
          <%= link_to genre.name, customers_genre_path(genre.id) %>
        </td>
      </tr>
    <% end %>
  </table>